server.port=8443
server.ssl.enabled=true
server.ssl.key-store=sts-mock.p12
server.ssl.key-store-password=password
server.ssl.key-password=password

# Database Configuration (Default: SQLite)
db.driver-class-name=org.sqlite.JDBC
db.url=jdbc:sqlite:mock_server.db
db.username=
db.password=

# Example PostgreSQL Configuration (Override via env vars or uncomment)
# db.driver-class-name=org.postgresql.Driver
# db.url=jdbc:postgresql://localhost:5432/sts_mock
# db.username=postgres
# db.password=password
