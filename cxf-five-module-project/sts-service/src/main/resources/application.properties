mock.server.url=https://localhost:8443/api
mock.server.ssl.trust-all=true

# Database Configuration (Default: SQLite)
db.driver-class-name=org.sqlite.JDBC
db.url=jdbc:sqlite:service_uetrs.db
db.username=
db.password=

# Example PostgreSQL Configuration
# db.driver-class-name=org.postgresql.Driver
# db.url=jdbc:postgresql://localhost:5432/sts_service
# db.username=postgres
# db.password=password
